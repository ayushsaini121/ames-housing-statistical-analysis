# Install required libraries
install.packages(c(
  "tidyverse",
  "modeldata",
  "fitdistrplus",
  "moments",
  "caret"
))

# Load libraries
library(tidyverse)
library(modeldata)
library(fitdistrplus)
library(moments)
library(caret)
